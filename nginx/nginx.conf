
#Modifier dans /etc/nginx/nginx.conf :
	##
	# Virtual Host Configs
	##

#	include /etc/nginx/conf.d/*.conf;
#	include /etc/nginx/sites-enabled/*;
	
#en	
	##
	# Virtual Host Configs
	##

#	#include /etc/nginx/conf.d/*.conf;
#	#include /etc/nginx/sites-enabled/*;
#	include /home/fbourgue/0-wsp/webserv/nginx/nginx.conf;


#server {
#	listen 80 default_server;
#	listen [::]:80 default_server;

	# SSL configuration
	#
	# listen 443 ssl default_server;
	# listen [::]:443 ssl default_server;
	#
	# Note: You should disable gzip for SSL traffic.
	# See: https://bugs.debian.org/773332
	#
	# Read up on ssl_ciphers to ensure a secure configuration.
	# See: https://bugs.debian.org/765782
	#
	# Self signed certs generated by the ssl-cert package
	# Don't use them in a production server!
	#
	# include snippets/snakeoil.conf;

	#root /var/www/html;
#	root /home/fbourgue/0-wsp/webserv/htdocsdefault

	# Add index.php to the list if you are using PHP
#	index index.html index.htm index.nginx-debian.html;

#	server_name _;

#	location / {
		# First attempt to serve request as file, then
		# as directory, then fall back to displaying a 404.
#		try_files $uri $uri/ =404;
#	}

	# pass PHP scripts to FastCGI server
	#
	#location ~ \.php$ {
	#	include snippets/fastcgi-php.conf;
	#
	#	# With php-fpm (or other unix sockets):
	#	fastcgi_pass unix:/run/php/php7.4-fpm.sock;
	#	# With php-cgi (or other tcp sockets):
	#	fastcgi_pass 127.0.0.1:9000;
	#}

	# deny access to .htaccess files, if Apache's document root
	# concurs with nginx's one
	#
	#location ~ /\.ht {
	#	deny all;
	#}
#}


# Virtual Host configuration for example.com
#
# You can move that to a different file under sites-available/ and symlink that
# to sites-enabled/ to enable it.
#
	server {
		satisfy any;
	    allow all;
	    
    log_subrequest on;
    rewrite_log on;
    error_log /var/log/nginx/error.log notice;
    
		listen 127.0.0.2:80;
		#listen [::]:80;
	
		server_name s1.org;
	
		root /home/fbourgue/0-wsp/webserv/htdocs;
		index index.html;
	
		location /cgi-bin_fred/ {
			satisfy any;
			allow all;
			root /home/fbourgue/0-wsp/webserv/cgi-bin/;
			#alias /home/fbourgue/0-wsp/webserv/cgi-bin/;
			rewrite /cgi-bin_fred/(.*) /$1 break;
			
			
			include snippets/fastcgi-php.conf;
			fastcgi_param HTTP_PROXY "";
    fastcgi_split_path_info ^(.+\.php)(/.+)$;
    include fastcgi_params;
    fastcgi_param PATH_INFO $fastcgi_path_info;
			fastcgi_pass unix:/run/php/php8.2-fpm.sock;
			
		}
		location ~* /static/ {
			root /home/fbourgue/0-wsp/webserv/htdocs;
			rewrite /static/(.*) /$1 break;
		}
	}
